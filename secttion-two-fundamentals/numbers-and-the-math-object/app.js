/*
  Working with numbers and the math objectd
*/

const num1 = 100;
const num2 = 200;

// Simple math with numbers
val = num1 + num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2; // modulo


// Math Object
val = Math.PI;
val = Math.E;
val = Math.round(2.8); // rounds
val = Math.ceil(2.4); // rounds up
val = Math.floor(2.8); // rounds up
val = Math.sqrt(64);
val = Math.pow(8,2);
val = Math.min(2,1,3,4,5,6,7,42,-30);
val = Math.max(2,1,3,4,5,6,7,42,-30);
val = Math.random();
val = Math.floor(Math.random() * 20 + 1); // rand 1:20



// Output
console.log(val);
console.log(typeof val);
